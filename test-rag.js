import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); import { createClient } from '@supabase/supabase-js'; import { embedBatch } from './lib/embeddings.mjs'; const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const [embedding] = await embedBatch([What's the cutoff for CSE at NIT Warangal?]); const { data, error } = await supabase.rpc('match_jee_josaa_2025', { query_embedding: embedding, match_count: 6, min_rank: null, seat_type_filter: null, gender_filter: null, quota_filter: null, exam_filter: null, year_filter: null, state_filter: null }); if (data) { data.forEach(d => console.log(d.metadata.institute, '|', d.metadata.program, '|', d.metadata.seat_type, '|', d.metadata.closing_rank, '| sim:', d.similarity)); } } run();
